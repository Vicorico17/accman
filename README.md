# Orbit

A small, browser-based social account and niche manager for TikTok, Instagram, and YouTube.

## Run it

Open `index.html` directly in a browser, or serve the folder with any static server. Data is stored in browser local storage, so adding and removing accounts or niches persists between visits on the same browser.
